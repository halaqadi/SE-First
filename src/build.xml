<project default="compile">
	<propery name="src.java.dir" location="./src/main/java/converter"/>
	<propery name="src.testdir" location="./src/main/test"/>
	<propery name="result.dir" location="./target"/>
	<propery name="result.javaclasses.dir" location="${result.dir}/classes"/>
	<propery name="result.testclasses.dir" location="${result.dir}/testclasses"/>
	<property name="result.jar.file" location="./MyProduct.jar"/>
	
	<target name="compile">
		<mkdir dir="${result.javaclasses.dir}"/>
		<javac srcdir="${src.java.dir}" destdir="${result.javaclasses.dir}" debug="true" includeantruntime="false">
		</javac>
		</target>
		
		</project>